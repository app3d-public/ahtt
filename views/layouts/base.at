block vars
doctype html
html(lang='_("lang")')
  head
    title= site_name
    block head
      | <!-- default head from layout -->
    style
        include style.css
  body
    header
      h1= site_name
    nav.navbar
      include includes/menu.at

    block content
      .container
        p Default content from layout

    footer
      | © 2025 App3D

mixin button(const char* text)
  button.btn(type=button)= text
    block
