extends layouts/base.at

prepend vars
    include includes/external.at

block prepend head
    | <!-- child prepends to head -->

block content
    .content
    +button("Click me")
        p block
    p Hello #{data.name}
    | Plain text line kept as-is
    .textblock.
        This is a text block line 1

        and line 2 (keeps newlines)
    - int x = 42;
    - for(int i = 0; i < 2; i++)
        p= x
        - x += 1;
        - for(int j = 0; j < 2; j++)
            p= x